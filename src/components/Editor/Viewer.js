import React from "react";
import { Typography } from "antd";

import styles from "./Editor.module.css";

const Viewer = () => {
  return (
    <div className={styles.new}>
      <Typography.Title level={2}>Viewer</Typography.Title>
    </div>
  );
};

export default Viewer;
